drop function measuregroup(bioassay in integer, measuregroup in integer);
drop function measuregroup_inv1(iri in varchar);
drop function measuregroup_inv2(iri in varchar);

--------------------------------------------------------------------------------

drop table measuregroup_genes;
drop table measuregroup_proteins;
drop table measuregroup_bases;
